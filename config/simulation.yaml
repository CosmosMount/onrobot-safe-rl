# Simulation experiment profile. Inherits config/common.yaml.

reward_profile: upstream

train:
  experiment_name: post_e05_safe_reward
  max_steps: 10000
  save_dir: saved/experiments/post_e05_safe_reward
  metrics_interval: 10
  wandb_run_name: post_e05_safe_reward
